import { HeaderContainer, Toolbar } from './style';

const Header = () => {
  return (
    <HeaderContainer>
      <Toolbar>

        <div style={{ flexGrow: 1 }} />

      </Toolbar>
    </HeaderContainer>
  );
};

export default Header;
